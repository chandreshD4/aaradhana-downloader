name: Build RajuBhai VIP APK
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17'
          
      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          
      # पहले नया एंड्रॉयड फ़ोल्डर सही नाम से ताज़ा बनाएंगे
      - name: Re-generate Android Folder Safely
        run: |
          rm -rf android
          flutter create --org com.example --project-name=aaradhana_downloader --platforms=android .
          
      # फ़ोल्डर बनने के तुरंत बाद जादुई कोड जोड़ेंगे ताकि कोई फाइल मिसिंग न हो
      - name: Force Global SDK 34 for All Plugins
        run: |
          cat << 'SUB' >> android/build.gradle
          subprojects {
              afterEvaluate { project ->
                  if (project.hasProperty("android")) {
                      project.android {
                          compileSdkVersion 34
                          targetSdkVersion 34
                      }
                  }
              }
          }
          SUB
          
      - name: Install Project Dependencies
        run: flutter pub get
        
      - name: Build VIP APK Freshly
        run: flutter build apk --release
        
      - name: Upload VIP APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: aradhana-vip-apk
          path: build/app/outputs/flutter-apk/*.apk
